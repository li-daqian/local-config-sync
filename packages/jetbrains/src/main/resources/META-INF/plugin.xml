<idea-plugin>
    <id>io.github.localconfigsync.jetbrains</id>
    <name>Local Config Sync</name>
    <vendor email="hi@lidaqian.me" url="https://github.com/li-daqian/local-config-sync">Li DaQian</vendor>
    <description><![CDATA[
        <p>
          Keep project-local overlay configuration inside your project for convenient editing,
          while excluding it from the project's Git history and synchronizing it through a
          user-configured Git or local-folder repository.
        </p>
        <p>The plugin provides:</p>
        <ul>
          <li>project setup and repository mapping;</li>
          <li>safe manual synchronization with conflict detection;</li>
          <li>Git authentication checks; and</li>
          <li>sync status in the IDE status bar.</li>
        </ul>
        <p>
          The plugin includes a compatible CLI bundle and detects common Node.js 20+
          installations automatically. Advanced executable overrides are available under
          <em>Settings | Tools | Local Config Sync</em>. The plugin does not store Git credentials
          or synchronize detected secret files by default.
        </p>
    ]]></description>
    <change-notes><![CDATA[
        <p>Initial release.</p>
        <ul>
          <li>Set up project-to-repository mappings.</li>
          <li>Run safe manual synchronization from the IDE.</li>
          <li>Verify Git authentication and view synchronization status.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
                parentId="tools"
                id="io.github.localconfigsync.settings"
                displayName="Local Config Sync"
                instance="io.github.localconfigsync.jetbrains.settings.LocalConfigConfigurable"/>
        <toolWindow
                id="Local Config Sync"
                anchor="right"
                icon="/icons/localConfigSync.svg"
                factoryClass="io.github.localconfigsync.jetbrains.toolwindow.LocalConfigToolWindowFactory"/>
        <statusBarWidgetFactory
                id="LocalConfigSyncStatus"
                implementation="io.github.localconfigsync.jetbrains.status.LocalConfigStatusWidgetFactory"/>
        <notificationGroup id="Local Config Sync" displayType="BALLOON" isLogByDefault="true"/>
    </extensions>

    <actions>
        <action id="LocalConfigSync.Setup"
                class="io.github.localconfigsync.jetbrains.actions.SetupAction"
                text="Setup Local Config Sync"
                description="Create a mapping for the current project">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="LocalConfigSync.Sync"
                class="io.github.localconfigsync.jetbrains.actions.SyncAction"
                text="Sync Local Config Now"
                description="Safely pull and push local configuration">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="LocalConfigSync.Auth"
                class="io.github.localconfigsync.jetbrains.actions.GitAuthAction"
                text="Authenticate Local Config Git Repository"
                description="Verify SSH, credential helper, or GitHub CLI authentication">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
